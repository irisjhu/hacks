import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  centerItems: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  topBottomPadding: {
    padding: "50px 30px",
  },
}));
